using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CommanderShowPopup  /*ICommandTest*/
{
    /*private readonly ViewNetworkChecker _viewNetworkChecker;

    public CommanderShowPopup(ViewNetworkChecker viewNetworkChecker)
    {
        _viewNetworkChecker = viewNetworkChecker;
    }

    public void Execute(object parameter)
    {
        if(parameter is bool isShow) 
        {
            _viewNetworkChecker.PopupNetworkError(isShow);
        }

    }

    public void Undo(object parameter)
    {
    }*/
}
