using Doozy.Runtime.UIManager.Containers;
using Sirenix.Utilities;
using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UI.Enum;
//using UI.Splash;
using UnityEngine;

public class ViewNetworkChecker : MonoBehaviour
{
   /* public ViewPopupNetworkError ViewPopupNetworkError;
    public ControllerNetworkChecker ControllerNetworkChecker;

    private ICommand _popupNetworkError;

    private void Awake()
    {
        _popupNetworkError = new RelayCommand<bool>(p => true,
            PopupNetworkError);

        ControllerNetworkChecker.NetworkErrorCommand = _popupNetworkError;
    }

    public void PopupNetworkError(bool isShow)
    {
        if (isShow)
        {
            Debug.Log("show error");
        }
        else
        {
            Debug.Log("hide error");
        }

    }*/
}

